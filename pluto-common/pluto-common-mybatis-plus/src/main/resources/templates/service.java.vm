package ${package.Service};

import com.baomidou.mybatisplus.core.metadata.IPage;
import ${package.Entity}.${entity};
import ${superServiceClassPackage};
import com.mine.${package.ModuleName}.vo.${entity}VO;
import com.mine.${package.ModuleName}.dto.${entity}DTO;

import java.util.List;

/**
 * @description: 【${table.comment}】代理层
 * @author: Generated by ${author}
 * @create: ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entity}>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entity}> {

    List<${entity}VO> list(${entity}DTO dto);

    ${entity}VO detail(Long id);

    void insert(${entity}DTO dto);

    void update(${entity}DTO dto);

    void delete(Long id);

}
#end
